// We will be given an interval e.g-{1,3} starts at one and end at 3 now we will merge all the interval to minimum 

#include<iostream>
#include<algorithm>
using namespace std;
int main(){
     int n;
    cin>>n;
  int arr[n];

    for(int i=0;i<n;i++){
        cin>>arr[i];
    } 

    sort(arr,arr+n);



}